import { Projects } from "#content";

export interface ProjectStacksIconProps {
  stacks: Projects["stacks"];
}

export const ProjectStacksIcon = ({ stacks }: ProjectStacksIconProps) => {
  return <div>Icons</div>;
};
